# Editorial Blueprint: Quality Retry / Reader Repair consistency

## Reproduced cause

At base main `cd9f581986d18822bf0c3c6a6e549c512424923a`, Run226 removed legacy base Writer quotas, but later Run228, reader-value bridge and Run208 wrappers appended contradictory numeric editorial instructions. Retry feedback added another copy before the Writer received the assembled prompt.

## Change

Update the originating prompt contracts in those three existing modules. Select mechanisms, terminology, enumeration, paragraph length and explanation bridges by Central Conclusion / Capability Boundary / Reader Decision. Remove one-mechanism, three-item, per-paragraph concept, opening-character and paragraph-switch quotas. Preserve necessary names, conditions and evidence; remove only irrelevant inventory and repetition. Do not append explanation bridges mechanically after each technical paragraph.

The canonical specification and Run405 supplement are updated together. Historical Run345 investigation notes are historical evidence, not the current editorial authority.

## Invariants

- No change to retry eligibility, owner separation, spent flags, provider routing or budget.
- No change to Publication / Fact / Evidence / Reader gate implementations or thresholds.
- Existing publication policy fingerprint already covers all three modified runtime files.
- No new provider call, generation workflow, schema, runtime layer or mutable quality-memory store.
- Daily remains paused; implementation does not dispatch article generation or note drafting.
- A main merge invokes the existing Note Ready policy reconciliation workflow; it can revoke stale queue eligibility. It does not authorize automatic re-certification or note publication.

## Validation

- Targeted legacy-contract and retry-safety suite: 66 passed.
- New assembled editorial-layer regression covers first pass, reader-only and combined score/reader feedback, final-surface failures and bounded Evidence-safe repair.
- Full Production runtime prompt assembly: first pass, reader-only retry and combined retry all pass with socket calls blocked (3 cases, zero network calls).
- Integration stability guard and repository falsification guard: PASS.
- Synthetic smoke through production_pipeline: 30/30, zero critical failures, production_write_isolation=true.
- Full deterministic regression: 2,522 passed (one pre-existing Pillow deprecation warning).
- Initial feature-branch push was blocked by automatic approval review; no alternate upload route was used. The user subsequently explicitly authorized the merge in this conversation.
- PR CI must pass before main integration. The PR and merge history record the remote integration result.

This proves instruction consistency and deterministic safety, not Human Appeal improvement in a newly generated article. A separate bounded live validation must still re-run every gate before any quality-success claim.
