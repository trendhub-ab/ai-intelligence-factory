# Run177 Paid Funnel Alignment

## Final funnel

Free note article → tracked CTA → recruitment LP → note membership → member-only start article → Google Form → AI Decision Intelligence (Notion).

## Runtime contract

- `SUBSCRIPTION_LANDING_URL` remains the only landing-page source of truth.
- CTA tracking remains owned by `pipeline.build_subscription_tracking_url`.
- Run177 changes reader-facing CTA copy only.
- No subscriber PII is introduced into GitHub attribution artifacts.
- No Gemini/model call is added.
- Daily remains PAUSED; ONE-SHOT remains manual only.

## Reader-facing naming

- Product: `AI Decision Intelligence`
- Recurring summary: `会員向けDigest`
- Old public wording `意思決定DB＋月次サマリー` must not appear in the installed CTA.
