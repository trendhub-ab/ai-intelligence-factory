__pycache__/
*.py[cod]

# Local/test/private artifacts. Public commit対象とprivate artifact対象を分離し、
# 一時成果物は誤ってRepositoryへcommitしない。
regen_test_outputs/
regression_suite_runs/
regression_suite/fixtures/
observed_history/
eyecatch_images/
note_eyecatch_images/
monthly_digests/
review_candidates/
quality_failures/
gate_history/
regression_cases_pending/
migration_outputs/

# Aggregate runtime learning state; persisted by GitHub Contents API, not manual commits.
source_roi_history/
subscription_attribution/
.runtime/

# note browser authentication state is a credential. Never commit local capture output.
note_storage_state*.json
note_storage_state*.b64
.note-session-chrome/

# Per-run private human-audit artifact; never commit/package as source state.
article_audit/
# Repository hygiene (Run127 cleanup)
.pytest_cache/
*.zip
*_work/
*_fresh_verify/
SHA256SUMS.txt
RUN*_MUTATION_NEGATIVE_CONTROL_*.log
inventory_bootstrap_artifacts/*
!inventory_bootstrap_artifacts/README.md
