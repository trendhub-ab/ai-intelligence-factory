name: Ops Run225 Stock Lifecycle Plan

on:
  push:
    branches:
      - ops-run225-stock-lifecycle-20260904
    paths:
      - '.github/workflows/ops-run225-stock-lifecycle-plan-20260904.yml'

permissions:
  contents: read

concurrency:
  group: aiif-stock-lifecycle
  cancel-in-progress: false

jobs:
  plan:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - name: Install minimal dependency
        run: python -m pip install requests
      - name: Read-only lifecycle plan
        run: python stock_lifecycle_reconcile.py
        env:
          TZ: 'Asia/Tokyo'
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          NOTION_DATA_SOURCE_ID: ${{ secrets.NOTION_DATA_SOURCE_ID }}
          NOTION_API_VERSION: '2026-03-11'
          STOCK_FRESH_DAYS: '30'
          STOCK_ARCHIVE_DAYS: '90'
          STOCK_LIFECYCLE_MAX_RECORDS: '5000'
          STOCK_LIFECYCLE_NOTION_PACING_SECONDS: '0.34'
      - name: Safety assertion
        run: echo 'PLAN_ONLY=true ZERO_GEMINI_CALLS=true PUBLIC_RELEASE=false RECORD_DELETION=false'
