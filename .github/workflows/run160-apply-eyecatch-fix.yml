name: Run160 Workflow Bootstrap Check

on:
  push:
    branches:
      - fix/run160-public-eyecatch-unification

permissions:
  contents: write

jobs:
  bootstrap-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check runner
        run: echo RUN160_WORKFLOW_OK
