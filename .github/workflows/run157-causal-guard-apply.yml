name: Run157 Workflow Trigger Check

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  trigger-check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Run157 PR trigger OK"
