name: Run157 Workflow Trigger Check

on:
  push:
    branches:
      - fix/run157-causal-inference-evidence-guard

permissions:
  contents: read

jobs:
  trigger-check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Run157 trigger OK"
