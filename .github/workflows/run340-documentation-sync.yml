name: Run340 canonical Run339b purchase-funnel documentation sync

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type SYNC_RUN339B_CANONICAL_PURCHASE_FUNNEL'
        required: true
        type: string
  issue_comment:
    types: [created]

permissions:
  contents: write

concurrency:
  group: run340-canonical-purchase-funnel-doc-sync
  cancel-in-progress: false

jobs:
  sync:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'issue_comment' &&
       github.run_attempt == 1 &&
       github.event.issue.number == 71 &&
       github.event.issue.pull_request == null &&
       github.event.comment.user.login == 'trendhub-ab' &&
       github.actor == 'trendhub-ab' &&
       github.event.comment.body == '/aiif docs sync-run339b')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - name: Validate exact authorization
        env:
          EVENT_NAME: ${{ github.event_name }}
          CONFIRM: ${{ inputs.confirm }}
          COMMENT: ${{ github.event.comment.body }}
        run: |
          set -euo pipefail
          if [ "$EVENT_NAME" = 'workflow_dispatch' ]; then
            [ "$CONFIRM" = 'SYNC_RUN339B_CANONICAL_PURCHASE_FUNNEL' ] || exit 1
          else
            [ "$COMMENT" = '/aiif docs sync-run339b' ] || exit 1
          fi
      - name: Falsify deterministic Run340 documentation migration
        run: python -m unittest tests.test_run340_documentation_sync -v
      - name: Apply Run340 canonical documentation sync
        run: python run340_documentation_sync.py
      - name: Enforce exact output file set
        run: |
          python - <<'PY'
          import subprocess
          expected = {
              'README.md',
              'AI_Intelligence_Factory_最終仕様書.md',
              'docs/reference/RUN335_NOTE_ONBOARDING_CUSTOMER_SURFACE_BASELINE.md',
              'docs/reference/RUN339B_NOTE_PUBLIC_PURCHASE_FUNNEL_BASELINE.md',
              '.github/workflows/note-membership-public-surface-diagnostic.yml',
              'run338_membership_public_surface_diagnostic.py',
              'tests/test_run338_membership_public_surface_diagnostic.py',
              '.github/workflows/run340-documentation-sync.yml',
              'run340_documentation_sync.py',
              'tests/test_run340_documentation_sync.py',
          }
          raw = subprocess.check_output(
              ['git', '-c', 'core.quotepath=false', 'status', '--short'],
              text=True,
          )
          observed = set()
          for line in raw.splitlines():
              if not line.strip():
                  continue
              path = line[3:]
              if ' -> ' in path:
                  path = path.split(' -> ', 1)[1]
              observed.add(path)
          if observed != expected:
              raise SystemExit(f'Run340 unexpected changed paths: observed={sorted(observed)!r} expected={sorted(expected)!r}')
          print('RUN340_OUTPUT_FILESET=PASS')
          PY
      - name: Create deterministic output branch
        env:
          OUTPUT_BRANCH: run340-doc-sync-output
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git switch -c "$OUTPUT_BRANCH"
          git add -A
          git commit -m 'Run340: promote Run339b public purchase-funnel baseline'
          git push origin "HEAD:refs/heads/$OUTPUT_BRANCH" --force
      - name: Summarize zero-provider contract
        run: |
          {
            echo '### Run340 canonical purchase-funnel documentation sync'
            echo '- output branch: `run340-doc-sync-output`'
            echo '- note onboarding saved-state baseline: `Run335`'
            echo '- public purchase-funnel baseline: `Run339b`'
            echo '- note mutations: `0`'
            echo '- Gemini/model calls: `0`'
            echo '- Notion writes: `0`'
            echo '- one-shot Run338/Run340 machinery retired in output branch: `true`'
          } >> "$GITHUB_STEP_SUMMARY"
