name: Daily Intelligence & Content Pipeline [ONE-SHOT STOP]

on:
  push:
    branches:
      - daily-once-20260901

concurrency:
  group: ai-intelligence-gemini-budget
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  stop-recursive-runs:
    runs-on: ubuntu-latest
    steps:
      - name: Stop recursive one-shot Daily runs
        run: echo "One-shot Daily completed. Recursive push-triggered runs are intentionally cancelled."
